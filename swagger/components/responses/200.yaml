description: Successfully patched a contact!
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 200
        message:
          type: string
          example: Success
        data:
          type: object
          required:
            - result
          properties:
            result:
              type: string
              example: 'Successfully patched a contact!'
